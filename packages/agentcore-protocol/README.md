# agentcore-protocol

Shared HTTP/SSE protocol DTOs and client library for AgentCore.

This package intentionally avoids server/runtime dependencies such as FastAPI,
Uvicorn, Torch, Transformers, SGLang, and LMDeploy.
